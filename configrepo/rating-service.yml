spring:
  profiles: ratingservice
  datasource:
    url: jdbc:mysql://localhost:3306/rating
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: password
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
        ddl-auto: validate
